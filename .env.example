PORT=3000
DATABASE_URL="file:./dev.db"

# Logging
# LOG_LEVEL: debug | info | warn | error (default: debug, or info when NODE_ENV=production)
LOG_LEVEL=debug
# NODE_ENV=production emits one-line JSON logs; otherwise human-readable output
NODE_ENV=development
